# signlanguage_detection
A real-time sign language recognition system that classifies 30 hand gestures (A–Z, Hello, Yes, No, Thanks) via webcam. MediaPipe extracts 21 hand landmarks (63 features) fed into a Random Forest classifier. Trained on 6,000 manually collected samples. Features include prediction smoothing, live confidence display, and a sentence builder.
